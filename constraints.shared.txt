# Shared venv constraints (01_environment_setup–47_asyncio_basics; exclude 52_customer_service_platform/customer_service_platform)
#
# Use this file whenever you install new packages into `/ai-training/.venv`,
# to prevent the LangChain stack from being upgraded to `langchain-core>=1.0.0`.
#
# Example:
#   .venv/bin/pip install -c constraints.shared.txt <some-package>
#
# If a new package *requires* `langchain-core>=1.0.0`, pip will fail fast instead
# of silently upgrading LangChain and breaking old notebooks.

langchain==0.3.27
langchain-core==0.3.75
langchain-community==0.3.29
langchain-openai==0.3.32
langchain-text-splitters==0.3.11

langgraph==0.6.6
langgraph-prebuilt==0.6.4
langgraph-checkpoint==2.1.1
langgraph-sdk==0.2.6

# Keep the client compatible with the pinned LangChain versions.
langsmith==0.5.0

