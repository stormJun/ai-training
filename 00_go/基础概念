本地编译
Go 设计时就考虑了多核 CPU 和并发。
Go 有垃圾回收，不需要像 C/C++ 那样手动释放内存


Go 的 GC 主要是：
  并发三色标记清除垃圾回收

 Go 用三种颜色来表示对象状态：
  白色：还没检查，可能是垃圾
  灰色：已经发现，但它引用的对象还没检查完
  黑色：已经检查完，确认是存活对象


Java 的运行方式大概是：
  Java 源代码
     ↓ 编译
  .class 字节码
     ↓ JVM 解释/即时编译执行
  机器码
  所以 Java 程序通常依赖 JVM

   Go 源代码
     ↓ go build 编译
  本地机器可执行文件
     ↓ 直接运行

 Go 虽然没有 JVM，它有自己的 runtime 运行时

Go runtime 负责很多事情：
- 垃圾回收 GC
- goroutine 调度
- channel 管理
- 栈自动扩容
- 内存分配
- panic/recover
- 定时器
- 网络轮询


  Go 源代码是跨平台的
  Go 编译器支持交叉编译

可以在 Windows 上编译出 Linux 版本，也可以在 macOS 上编译出
  Windows 版本