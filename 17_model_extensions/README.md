# 模型扩展专题

本父目录当前保留模型扩展相关的两个主题，目的是减少仓库顶层目录数量，同时保留各主题原本的资料结构。原 `03_reinforcement_learning/` 已并入根目录下的 `22_reinforcement_learning_notes/`。

## 子目录

- `01_slm_optimization/`
  - 小模型优化
- `02_multimodal_clip_search/`
  - CLIP 图像搜索

## 建议学习顺序

1. 先看 `01_slm_optimization/`
2. 再看 `02_multimodal_clip_search/`
3. 强化学习请直接查看 `../22_reinforcement_learning_notes/`

## 说明

- 该父目录只做聚合，不拆分各子主题内部结构。
- 子目录里的命令默认以仓库根目录或各自子目录为起点，具体以子目录 README 为准。
