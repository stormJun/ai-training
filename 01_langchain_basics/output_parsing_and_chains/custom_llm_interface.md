# 自定义大模型接口

这一节原始 notebook 内容很少，核心意思是：

在实际工作中，除了直接调用公开模型服务，还经常会接入：

- 私有部署模型
- 公司内部网关
- OpenAI 兼容接口
- 第三方代理服务

因此需要掌握“如何自定义模型接口参数”。

## 常见可配置项

- `base_url`
- `api_key`
- `model`
- `temperature`
- 超时、重试、代理等客户端参数

## 实际意义

这部分内容和后面的 Prompt、Parser、Chain 不冲突，它更像是 LangChain 或 OpenAI 封装真正落地时的接入层准备。
